import { Link } from "react-router-dom";

function NavBar() {
  return (
    <nav>
      <button type="button" className="btn btn-primary">
        Primary
      </button>
    </nav>
  );
}

export default NavBar;
